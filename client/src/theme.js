import { createTheme } from "@mui/material/styles";

// Create a custom theme
const theme = createTheme({
	typography: {
		fontFamily: "Montserrat, sans-serif", // Replace 'Your-Preferred-Font' with the actual font family you want to use
		// You can further customize typography options here, like fontSize, fontWeight, etc.
	},
});

export default theme;
